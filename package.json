{
  "name": "obsidian-kbd",
  "version": "0.0.3",
  "description": "Wrap selected text with <kbd> tags and style them in Obsidian.",
  "author": "Keith Walsh",
  "license": "MIT",
  "scripts": {
    "dev": "node esbuild.config.mjs dev",
    "build": "tsc -noEmit && node esbuild.config.mjs production",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:dev": "node scripts/test.js",
    "test:dev:watch": "node scripts/test.js --watch",
    "test:dev:coverage": "node scripts/test.js --coverage"
  },
  "devDependencies": {
    "obsidian": "latest",
    "typescript": "^5.4.0",
    "esbuild": "^0.20.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "@testing-library/jest-dom": "^6.4.0",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
} 